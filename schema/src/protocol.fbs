include "packets.fbs";

namespace Nexus;

enum PacketType: ushort {
    CG_LOGIN_REQ = 0,
    GC_LOGIN_RES,
    CG_JOIN_REQ,
    GC_JOIN_RES,
    GC_SPAWN_ACTOR_NOTI,
    GC_REMOVE_ACTOR_NOTI,
    CG_CHANGE_MOVE_DIRECTION_REQ,
    GC_CHANGE_MOVE_DIRECTION_RES,
    GC_CHANGE_ACTOR_DIRECTION_NOTI,

    CG_LOGOUT_NOTI,
}
